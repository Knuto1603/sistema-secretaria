meta {
  name: LOGIN
  type: http
  seq: 1
}

post {
  url: {{SECURITY_API}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "decano@universidad.edu",
    "password": "password123",
    "device_name": "mi_laptop"
  }
}

script:post-response {
  bru.setVar("token", res.body.access_token);
}
