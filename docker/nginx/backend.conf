# ============================================================
# Nginx – Proxy para Laravel (PHP-FPM)
# ============================================================
server {
    listen 80;
    server_name _;

    root /var/www/html/public;
    index index.php;

    # Tamaño máximo de upload (ZIP historiales hasta 100MB)
    client_max_body_size 110M;

    # Logs
    access_log /var/log/nginx/backend_access.log;
    error_log  /var/log/nginx/backend_error.log;

    # DNS embebido de Docker: resuelve "backend" en cada request en vez de
    # cachear la IP al arrancar el worker (evita "Connection refused" tras
    # un redeploy que recrea el contenedor backend con otra IP interna).
    resolver 127.0.0.11 valid=10s;

    # Rutas de Laravel
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP-FPM
    location ~ \.php$ {
        set $upstream_backend backend:9000;
        fastcgi_pass $upstream_backend;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME /var/www/html/public$fastcgi_script_name;
        fastcgi_param PHP_VALUE "upload_max_filesize=110M \n post_max_size=110M";
        fastcgi_read_timeout 300;
        include fastcgi_params;
    }

    # Bloquear acceso a archivos ocultos
    location ~ /\. {
        deny all;
    }
}
