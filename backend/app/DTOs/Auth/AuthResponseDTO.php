<?php

namespace App\DTOs\Auth;

/**
 * Response DTO for authentication operations
 * Alias for LoginResponseDTO for consistency with naming convention
 */
class AuthResponseDTO extends LoginResponseDTO
{
    // Inherits all functionality from LoginResponseDTO
}
